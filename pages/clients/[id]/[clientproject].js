import { useRouter } from "next/router";

function SelectedClientProjectPage() {
  const router = useRouter();

  return (
    <div>
      <h2>This is getting out of hand Client in Client folder dynamically</h2>
    </div>
  );
}

export default SelectedClientProjectPage;
